import { ExistTransportCompanyPipe } from './exist-transport-company.pipe';

describe('ExistTransportCompanyPipe', () => {
  it('should be defined', () => {
    expect(new ExistTransportCompanyPipe()).toBeDefined();
  });
});
