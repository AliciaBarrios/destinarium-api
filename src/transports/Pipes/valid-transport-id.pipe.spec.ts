import { ValidTransportIdPipe } from './valid-transport-id.pipe';

describe('ValidTransportIdPipe', () => {
  it('should be defined', () => {
    expect(new ValidTransportIdPipe()).toBeDefined();
  });
});
