import { ExistAccomodationNamePipe } from './exist-accommodation-name.pipe';

describe('ExistAccommodationNamePipe', () => {
  it('should be defined', () => {
    expect(new ExistAccomodationNamePipe()).toBeDefined();
  });
});
