import { ValidAccommodationIdPipe } from './valid-accommodation-id.pipe';

describe('ValidAccommodationIdPipe', () => {
  it('should be defined', () => {
    expect(new ValidAccommodationIdPipe()).toBeDefined();
  });
});
